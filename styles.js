import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  message_gps: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    textAlign: "center",
    padding: 30,
  },

  text: {
    fontSize: 18,
    textAlign: "center",
    marginBottom: 10,
  },
});
